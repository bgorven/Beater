<Page
    x:Class="Beater.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Beater"
    xmlns:viewmodel="using:Beater.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <!--xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"-->

    <Page.Resources>
        <local:NumericScaleConverter Scale="0.00048828125"  x:Key="SamplesToPixelsConverter" />
        <local:BooleanInverter x:Key="BooleanInverter" />
        <local:BooleanToVisibilityConverter x:Key="VisibilityConverter" />
        <local:BooleanToVisibilityConverter Invert="true" x:Key="InverseVisibilityConverter" />
    </Page.Resources>
    
    <Page.DataContext>
        <viewmodel:SongViewModel />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal">
            <Button Name="Play" Command="{Binding PlayCommand}">Play</Button>
            <Button Name="Pause" Command="{Binding PauseCommand}">Pause</Button>
            <Button Name="Stop" Command="{Binding StopCommand}">Stop</Button>
            <Button Name="Save" Command="{Binding SaveCommand}">Save</Button>
            <Button Name="Trash" Command="{Binding TrashCommand}">Trash</Button>
            <TextBox Text="{Binding Title, Mode=TwoWay}" VerticalAlignment="Center">
                <TextBox.Resources>
                    <Style TargetType="TextBox" >
                        <Setter Property="Background" Value="{StaticResource ButtonBackgroundThemeBrush}"></Setter>
                        <Setter Property="BorderBrush"  Value="{StaticResource ButtonBackgroundThemeBrush}"></Setter>
                        <Setter Property="Foreground" Value="{StaticResource ButtonForegroundThemeBrush}"></Setter>
                    </Style>
                </TextBox.Resources>
            </TextBox>
            
            <TextBox Text="{Binding BPM, Mode=TwoWay}" VerticalAlignment="Center">
                <TextBox.Resources>
                    <Style TargetType="TextBox" >
                        <Setter Property="Background" Value="{StaticResource ButtonBackgroundThemeBrush}"></Setter>
                        <Setter Property="BorderBrush"  Value="{StaticResource ButtonBackgroundThemeBrush}"></Setter>
                        <Setter Property="Foreground" Value="{StaticResource ButtonForegroundThemeBrush}"></Setter>
                    </Style>
                </TextBox.Resources>
            </TextBox>
            <TextBox Text="{Binding Length, Mode=TwoWay}" VerticalAlignment="Center">
                <TextBox.Resources>
                    <Style TargetType="TextBox" >
                        <Setter Property="Background" Value="{StaticResource ButtonBackgroundThemeBrush}"></Setter>
                        <Setter Property="BorderBrush"  Value="{StaticResource ButtonBackgroundThemeBrush}"></Setter>
                        <Setter Property="Foreground" Value="{StaticResource ButtonForegroundThemeBrush}"></Setter>
                    </Style>
                </TextBox.Resources>
            </TextBox>
        </StackPanel>
        <ListView ItemsSource="{Binding Tracks}" ScrollViewer.HorizontalScrollMode="Disabled" ScrollViewer.VerticalScrollMode="Auto" Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        
                        
                        <TextBlock Text="{Binding Name}" Width="100" Grid.Column="0"/>
                        <ListView ItemsSource="{Binding Pattern}" Grid.Column="1" ScrollViewer.HorizontalScrollMode="Auto" ScrollViewer.VerticalScrollMode="Disabled">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListViewItem">
                                                <ListViewItemPresenter ContentMargin="0"
                                                                   Padding="0" />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListView.ItemContainerStyle>

                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <ListView ItemsSource="{Binding Beats}" 
                                              ScrollViewer.HorizontalScrollMode="Disabled" ScrollViewer.VerticalScrollMode="Disabled">
                                        <ListView.Background>
                                            <LinearGradientBrush EndPoint="0,0.5" StartPoint="1,0.5">
                                                <GradientStop Color="DarkMagenta"  Offset="0"/>
                                                <GradientStop Color="Black" Offset="1"/>
                                            </LinearGradientBrush>
                                        </ListView.Background>
                                        <ListView.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ListView.ItemsPanel>

                                        <ListView.ItemContainerStyle>
                                            <Style TargetType="ListViewItem">
                                                <Setter Property="Padding" Value="0"/>
                                                <Setter Property="Margin" Value="0"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="ListViewItem">
                                                            <ListViewItemPresenter ContentMargin="0"
                                                                   Padding="0" />
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </ListView.ItemContainerStyle>
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <ToggleButton IsChecked="{Binding Beats, Mode=TwoWay}"
                                                    Width="{Binding BeatLength, Converter={StaticResource SamplesToPixelsConverter}}">
                                                    <Image />
                                                </ToggleButton>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                    
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Orientation="Horizontal" Grid.Row="2">
            <ComboBox SelectedValue="{Binding AssetFile, Mode=TwoWay}">
                <x:String>Closed Hat</x:String>
                <x:String>Crash Cymbal</x:String>
                <x:String>Floor Tom</x:String>
                <x:String>Kick</x:String>
                <x:String>Mid Hat</x:String>
                <x:String>Mid Tom</x:String>
                <x:String>Open Hat 1</x:String>
                <x:String>Open Hat 2</x:String>
                <x:String>Ride Cymbal</x:String>
                <x:String>Sample 1</x:String>
                <x:String>Sample 2</x:String>
            </ComboBox>
            <Button Command="{Binding AddTrackCommand}">+</Button>
        </StackPanel>
    </Grid>
</Page>
