<UserControl
    x:Class="Beater.Views.TrackBeatsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:view="using:Beater.Views"
    xmlns:viewmodel="using:Beater.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="100"
    d:DesignWidth="4000">
    
    <d:UserControl.DataContext>
        <viewmodel:TrackViewModel />
    </d:UserControl.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="25"/>
            <RowDefinition Height="75"/>
        </Grid.RowDefinitions>
        <view:CanvasItemsControl ItemsSource="{Binding Pattern}" Grid.Row="0" Height="25" 
                                 BindingPathLeft="Location" BindingConverterPosition="{StaticResource SamplesToPixelsConverter}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <view:PatternView DataContext="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </view:CanvasItemsControl>
    </Grid>
</UserControl>
